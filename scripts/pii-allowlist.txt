# pii-guard 허용목록 — 의도적으로 리포에 실린 값만.
#
# 형식: 한 줄에 값 하나, `값  # 사유` (사유 없는 등재 금지 — 왜 안전한지 남지 않으면
# 다음 사람이 판단할 수 없고, 허용목록은 그 순간 검사를 무력화하는 구멍이 된다).
#
# 예) 02-123-4567   # 의원 대표번호 — 환자 정보 아님, 홈페이지에 공개된 값
#
# 비어 있음 = 예외 없음 (2026-09-03 도입 시점 위반 0).
